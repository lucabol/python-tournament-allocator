# Example Environment Configuration for Azure App Service
# Copy this to your App Service Configuration -> Application settings

# Flask Configuration
FLASK_ENV=production
SECRET_KEY=change-this-to-a-secure-random-key-at-least-32-characters-long

# Optional: Custom Port (Azure uses 8000 by default)
# PORT=8000

# Optional: Enable Flask Debug Mode (NOT recommended for production)
# FLASK_DEBUG=false

# Optional: Database Configuration (if you migrate from file storage)
# DATABASE_URL=postgresql://user:password@server.postgres.database.azure.com/dbname

# Optional: Azure Storage Configuration (if using Azure Files or Blob)
# AZURE_STORAGE_CONNECTION_STRING=your-connection-string
# AZURE_STORAGE_CONTAINER=tournament-data

# Optional: Logging Level
# LOG_LEVEL=INFO

# Application Settings
# These can override defaults in constraints.yaml
# MATCH_DURATION=30
# BREAK_DURATION=10
# MAX_MATCHES_PER_TEAM_PER_DAY=5

# How to set these in Azure:
# Via Portal:
#   1. Go to your App Service
#   2. Navigate to Configuration -> Application settings
#   3. Click "New application setting" for each variable
#   4. Save and restart the app
#
# Via Azure CLI:
#   az webapp config appsettings set \
#     --resource-group <your-resource-group> \
#     --name <your-app-name> \
#     --settings FLASK_ENV=production SECRET_KEY=your-secret-key
